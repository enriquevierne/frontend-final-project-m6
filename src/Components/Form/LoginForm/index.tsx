import { Button } from "../Buttons"
import { Input } from "../Inputs"

export const FormLogin = () => {
    return(
        <form action="">
            <Input />
            <Input />
            <Button />
        </form>
    )
}